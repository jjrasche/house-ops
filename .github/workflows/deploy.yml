name: Deploy
on:
  workflow_dispatch:
  push:
    branches: [master, main]
    paths:
      - "frontend/**"
jobs:
  deploy:
    uses: jjrasche/platform/.github/workflows/build-deploy.yml@master
    with:
      app_name: house-ops
      context: frontend
    secrets: inherit
